Generate a random stochastic matrix:

A = rand(2, 2);
A = A./sum(A);

Let vector u = 1n,

A*u = 


...which is not a scalar multiple of u

Thus this cannot satisfy lamba * u
Therefore u is not a eigenvector of A
